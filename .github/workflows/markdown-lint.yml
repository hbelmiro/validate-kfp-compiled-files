# Placeholder: run markdown lint
# TODO: Add markdownlint config (e.g. .markdownlint.yaml)
name: markdown-lint

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  markdown-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # TODO: Install markdownlint-cli and run (e.g. markdownlint '**/*.md')
      - name: Run markdown lint
        run: |
          npm install -g markdownlint-cli
          markdownlint --version
          echo "Add markdownlint targets here (e.g. markdownlint '**/*.md' --ignore node_modules)"
